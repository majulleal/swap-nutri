import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Primaria — verde nutricional
  static const primary = Color(0xFF2E7D32);
  static const primaryLight = Color(0xFF60AD5E);
  static const primaryDark = Color(0xFF005005);

  // Secundaria — laranja energetico
  static const secondary = Color(0xFFFF8F00);
  static const secondaryLight = Color(0xFFFFC046);
  static const secondaryDark = Color(0xFFC56000);

  // Macros
  static const proteinas = Color(0xFFE53935);
  static const carboidratos = Color(0xFFFB8C00);
  static const gorduras = Color(0xFFFFD600);
  static const fibras = Color(0xFF43A047);
  static const calorias = Color(0xFF1E88E5);

  // Neutros
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const divider = Color(0xFFBDBDBD);
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF388E3C);
}
